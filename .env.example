# Variables de Entorno - GitHub MCP Server
# Copia este archivo como .env y completa con tus credenciales reales

# =============================================================================
# AUTENTICACIÓN MCP (REQUERIDO)
# =============================================================================
# Identificador de autenticación para el servidor MCP
AUTH_ID=tu_auth_id_aqui

# Token de autenticación para el servidor MCP  
AUTH_TOKEN=tu_auth_token_aqui

# =============================================================================
# TOKEN DE GITHUB (REQUERIDO)
# =============================================================================
# Token personal de GitHub con permisos apropiados
# 
# Para obtener tu token:
# 1. Ve a GitHub → Settings → Developer settings → Personal access tokens
# 2. Click "Generate new token (classic)"
# 3. Selecciona los permisos necesarios:
#    - repo (acceso completo a repositorios)
#    - workflow (para disparar workflows)
#    - admin:org (opcional, para organizaciones)
#
# Ejemplo: ghp_1234567890abcdef1234567890abcdef12345678
GITHUB_TOKEN=ghp_tu_token_de_github_aqui

# =============================================================================
# CONFIGURACIÓN OPCIONAL
# =============================================================================

# Puerto del servidor MCP (opcional, por defecto se usa puerto estándar MCP)
# MCP_PORT=3000

# Entorno de ejecución (development, production, etc.)
# NODE_ENV=development

# =============================================================================
# NOTAS IMPORTANTES
# =============================================================================
# 
# 1. NUNCA subas el archivo .env al control de versiones
# 2. El archivo .gitignore ya incluye .env para evitar esto
# 3. Mantén tus tokens seguros y no los compartas
# 4. Regenera tokens si sospechas que han sido comprometidos
# 5. Usa tokens con permisos mínimos necesarios
#
# =============================================================================
